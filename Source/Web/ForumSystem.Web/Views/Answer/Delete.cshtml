@model ForumSystem.Models.Answer

<div class="container">
    <div class="panel panel-danger">
        <h3>
            <p class="text-danger">Are you sure you want to delete this?</p>
        </h3>
        <div class="panel-body">
            @using (Html.BeginForm("Delete", "Answer", FormMethod.Post))
            {
                @Html.AntiForgeryToken()
                @Html.ValidationSummary("", new { @class = "text-danger" })
                <div>
                    @Html.Raw(Model.Content)
                </div>
                <div class="form-actions no-color">
                    <input type="submit" value="Delete" class="btn btn-danger" /> |
                    @Html.ActionLink("Back to List", "ViewAnswersOfPost", new { id = Model.PostId })
                </div>
            }
        </div>
    </div>
</div>
